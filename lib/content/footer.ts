import type { FooterContent } from "@/types/content";

import { footer } from "@/content/global/footer";

/** Returns site footer content. */
export function getFooter(): FooterContent {
  return footer;
}
